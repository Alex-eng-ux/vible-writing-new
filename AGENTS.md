# 项目工作约定

## 代码注释与文字说明

- 实现功能时补充必要的中文注释和说明：模块、公共类、公共函数、API、关键变量、参数、返回值、副作用以及重要业务约束都要说明。
- 对幂等、版本基线、身份互斥、租约与 fencing、事务边界等不直观逻辑，说明其目的、限制和失败条件。
- 不为简单赋值、明显的控制流或逐行代码添加注释；注释应解释“做什么”和“为什么”，不能重复代码本身。
- 代码标识符、类型名、接口名和错误码保持英文；注释、文档字符串和面向开发者的说明使用中文。
- 注释必须与实际实现一致；修改行为、接口或约束时同步更新相关注释，不得用注释掩盖未实现功能。

## 用户要求压缩时的开发日志

当用户明确说“压缩”时，在回复或执行后续压缩相关操作前，必须更新当天的 `docs/development-log-YYYY-MM-DD.md`。

- 先完整回看本次对话记录；不得只根据当前工作区、已有文档或最后一轮摘要编写日志。
- 当天已有日志时修订或追加同一文件，不新建重复日志。
- 准确记录用户主导提出、确认或修正的决策，特别是产品边界、工作流、交互语义、数据契约和技术取舍；不得将这些内容写成无归属的通用结论。
- 日志必须记录本次发现的当前不足、风险或未定稿决策；与“下一步”分开写。只记录有对话、文档或验证依据的不足，不凭空罗列风险；确实未发现时明确写“未发现已知不足”。
- 使用固定七段格式：`工作性质与范围`、`用户主导决策`、`关键规则与取舍`、`已完成产出`、`验证结果`、`当前不足与风险`、`当前未完成事项与下一步`。
- `用户主导决策` 使用“决策 / 原因与取舍 / 落点”表格；`当前不足与风险` 使用“问题 / 影响 / 验证状态”表格。其余章节使用短段落或列表，避免表格过宽。
- 只记录已经发生或已经验证的事实，并保留关键规则、例外和取舍；不要用空泛概述替代具体决策。
- 清楚区分“已写入设计文档”“已做文档级验证”和“已实现并测试”。没有应用代码、迁移或自动化测试时，必须如实说明。
- 使用 UTF-8 编码；写入后重新读取，确认日志能够正常读取。
